name: "Pull Request Review"

on:
  pull_request:
    # types: [labeled]
  push:
    branches:
      - main

jobs:
  request-reviewers:
    # if: ${{ github.event.label.name == 'request-review' }}
    uses: ./.github/workflows/pickaroo.yml
    secrets: inherit
    permissions:
      contents: read
    with:
      include_users: "jviall jasnoo hinzed1127 josephine-njooi"
      number_of_reviewers: 1
      channel_id: C09Q36G9HMX
